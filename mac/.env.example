# Nanomouse Build Configuration
# Copy this file to .env and fill in your values.

# 1. Code Signing Identity
# The name of your "Developer ID Application" certificate in Keychain Access.
# Run 'security find-identity -p codesigning -v' to list available identities.
# Example: "Developer ID Application: Zhang San (TEAMID123)"
# Leave empty to skip signing.
CODE_SIGN_IDENTITY=""

# 2. Notarization Profile
# The name of the keychain profile created via 'xcrun notarytool store-credentials'.
# Command: xcrun notarytool store-credentials "NanomouseProfile" --apple-id "email@example.com" --team-id "TEAMID123" --password "app-specific-password"
# Example: "NanomouseProfile"
# Leave empty to skip notarization.
NOTARY_KEYCHAIN_PROFILE=""

# 3. Development Team ID (Optional)
# If your certificate name implies your Team ID, the script will extract it automatically.
# Otherwise, or if you want to be explicit, set it here.
# Example: "S2D42Y29YT"
DEVELOPMENT_TEAM=""
